services:
  - type: web
    name: ndportx
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --worker-class gthread --workers 1 --threads 12 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
